/** Generate a Starblast asteroid map. Returns a `\n`-joined grid of digit strings (0=empty, 1–9=asteroid size). */
export declare function getMap(mapID: number, mapSize: number, mode: string): string
